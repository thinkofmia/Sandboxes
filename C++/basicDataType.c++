#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    //Initialize variables
    int it;
    long lg;
    char ch;
    float flt;
    double d;
    //Scan input
    scanf("%i %li %c %f %lf", &it, &lg, &ch, &flt, &d);
    //Returns output
    printf("%i \n", it);
    printf("%li \n", lg);
    printf("%c \n", ch);
    printf("%f \n", flt);
    printf("%lf \n", d);
    
    return 0;
}